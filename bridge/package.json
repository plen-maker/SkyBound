{
  "name": "skybound-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SkyBound EFB sim bridge — reads MSFS telemetry and pushes to phone via Firebase",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "firebase-admin": "^12.7.0",
    "node-simconnect": "^3.2.0"
  },
  "engines": { "node": ">=18" }
}
